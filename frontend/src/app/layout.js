import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "RutaTransporte | Gestión de horarios y reservas",
  description:
    "Frontend de la plataforma de transporte universitario para consultar horarios, reservar cupos y administrar rutas.",
  applicationName: "RutaTransporte",
  keywords: ["transporte universitario", "reservas", "horarios", "conductores", "rutas"],
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="es"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}